#include <iostream>
#include <vector>
#include <climits>
#include <queue>
#include <utility>
#include <algorithm>

using namespace std;

int f(int ind, int l, int r, vector<int>& nums) {
        
}

int maxCoins(vector<int>& nums) {
    int n = nums.size();
    return f(ind, 0, n-1, nums)
}

int main() {


    return 0;
}